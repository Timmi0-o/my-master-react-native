# Agent skills (архив)

Expo/RN skills перенесены в формат **Cursor** — корень репозитория:

```
.cursor/skills/
```

Индекс и когда какой skill читать: `my-master-react-native/.cursor/rules/project-rules.mdc` → раздел **Agent skills (Expo / RN)**.

Папка `skills/` здесь удалена, чтобы не дублировать `.cursor/skills/`.

---
description: Core React Native/Expo project rules
alwaysApply: true
---

# my-master-react-native project rules

## Stack and architecture

- Stack: Expo + React Native + TypeScript + `expo-router` + Uniwind + `heroui-native`.
- UI primitives and form controls must come from `heroui-native`; custom UI wrappers are allowed only when they compose `heroui-native`.
- Before implementing, check existing project patterns; if existing approach is weak, explicitly use a better one.

## Files, naming, and typing

- File names: kebab-case.
- Interfaces and object-only types: `I` prefix (example: `IProfileCard`).
- Boolean names: `is*` prefix only for booleans.
- Constants in `constants/`: UPPER_CASE.
- Avoid `any`; if unavoidable, limit scope and justify with a short comment.
- Public functions/hooks/components must have explicit return types.

## Data and code organization

- **Группировка страниц** (`components/pages/`): каждый экран — в своей подпапке; локальные `components/`, `hooks/`, `data/`, `helpers/` — рядом с экраном. Общее для фичи — только в корне модуля. Подробно: `.cursor/rules/page-folder-structure.mdc`.
- **Глобальные vs локальные утилиты**: в корневые `utils/`, `helpers/`, `constants/` — только то, что реально используется в **разных** модулях/фичах. Утилиты под конкретный экран, компонент или фичу (формат заголовка группы, группировка списка, маппинг полей карточки) — в локальную папку рядом с владельцем: `components/pages/<feature>/<screen>/utils/`, `components/shared/components/<component>/utils/`, `helpers/` модуля. Не раздувать общий `utils/` доменной логикой одного UI.
- Arrays for rendering (menu/stats/actions/options) store in nearby `data/` files, not inline in large screen components.
- Avoid long inline object types in component files; define reusable interfaces/types.
- Keep screens focused on composition; move business logic to hooks/helpers/actions.
- DRY first: duplicated logic must be extracted — но **не** через однострочные `utils/*`, `map*`, `resolve*`, `format*` и прочие микро-хелперы. Простую сборку строки, сортировку, фильтр, `toLocaleTimeString` и т.п. пиши **на месте** в компоненте или hook. Выносить только нетривиальную повторяющуюся логику (3+ осмысленных использования или сложный алгоритм).

## React Native UI/UX rules

- Uniwind/Tailwind в RN: только проверенные классы и паттерны из `.cursor/rules/uniwind-styling.mdc` (без arbitrary `text-[17px]`, `w-px`, `border-border/70`, shadow-only карточек).
- Формы экранов: `react-hook-form` + `zodResolver` + Zod-схема в `actions/<feature>/models/*.schema.ts` + `data/*-default-values.ts` + `hooks/use-on-submit-*.ts` (эталон: `actions/master/models/master-profile-edit.schema.ts` → `MasterProfileEditSchema` / `IMasterProfileEdit`; **не** класть схемы в `components/`).
- Every screen must handle safe areas (`SafeAreaView`) and use consistent page background including safe zones.
- Use `ScrollView`/`FlatList` deliberately: `FlatList` for long dynamic lists, `ScrollView` for short static layouts.
- No hardcoded magic spacing patterns repeated across the screen; keep spacing systematic.
- No raw inline SVG in JSX for screens; use assets/components.

## State and effects

- Do not use `useEffect` for derivable state; derive from props/state or compute in handlers.
- Side effects reused across screens must be extracted to shared hooks.
- Keep conditionals explicit and readable; avoid complex one-line boolean chains.

## API/actions and errors

- Do not call `fetch` directly inside screen components; use actions/helpers/api layer.
- API errors must be normalized and surfaced as user-readable states (empty/error/retry), not silent fails.
- Auth-related logic must go through project auth context/store/helpers only.
- Детальные правила actions/hooks/контракт API: `.cursor/rules/api-and-actions.mdc`.
- Хуки в `hooks/actions/`: строго `use{Entity}{Operation}` как у action (`useAppointmentChatGetOne`, не `useChatsList`). См. `api-and-actions.mdc`.
- Сквозной поток данных (бэк + клиент): `../../.cursor/rules/project-data-flow.mdc`.

## Agent skills (Expo / RN)

Скилы — **корень репо** `.cursor/skills/`. Перед задачей читать релевантный `SKILL.md`:

| Задача | Skill |
|--------|-------|
| Экраны, навигация, анимации, tabs | `building-native-ui` |
| Uniwind / className | `.cursor/rules/uniwind-styling.mdc` |
| Tailwind/NativeWind setup с нуля | `expo-tailwind-setup` |
| API, fetch, кэш, loaders | `native-data-fetching` |
| Query в URL, фильтры list | `react-native-query-filters` |
| Zod-схемы actions | `react-native-zod-schemas` |
| Expo SDK upgrade | `upgrading-expo` |
| Dev client | `expo-dev-client` |
| App Store / Play Store | `expo-deployment` |
| EAS workflows CI/CD | `expo-cicd-workflows` |
| EAS Update insights | `eas-update-insights` |
| Нативные модули | `expo-module` |
| App Clip | `add-app-clip` |
| Brownfield embed RN | `expo-brownfield` |
| API routes (EAS Hosting) | `expo-api-routes` |
| EAS Observe | `expo-observe` |
| SwiftUI / Compose UI | `expo-ui-swiftui`, `expo-ui-jetpack-compose` |
| DOM components | `use-dom` |

## Quality bar

- No hacks, no brittle workarounds, no overengineering.
- Priorities: correctness and edge cases first, speed second.
- Preserve accessibility basics (touch targets, text readability, loading/disabled states).
